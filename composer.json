{
    "name": "zicht/url-bundle",
    "description": "Url utilities for Symfony",
    "type": "symfony-bundle",
    "repositories": { },
    "authors": [
        {
            "name": "Zicht online",
            "email": "info@zicht.nl"
        }
    ],
    "license": "MIT",
    "require": {
        "php": "^7.0",
        "zicht/util": "^1.7",
        "doctrine/orm": "^2.5",
        "doctrine/common": "^2.4",
        "symfony/symfony": "^3.4",
        "zicht/framework-extra-bundle": "^8",
        "zicht/admin-bundle": "^5"
    },
    "require-dev": {
        "phpunit/phpunit": "^7",
        "zicht/standards-php": "^3.3"
    },
    "autoload": {
        "psr-0": {
            "": ["src/"]
        }
    },
    "autoload-dev": {
        "psr-0": {
            "": ["tests/"]
        }
    },
    "minimum-stability": "stable",
    "scripts": {
        "lint": [
            "phpcs -s src/Zicht/ --standard=vendor/zicht/standards-php/Zicht --extensions=php -p --parallel=$(nproc)"
        ],
        "lint-fix": [
            "phpcbf -s src/Zicht/ --standard=vendor/zicht/standards-php/Zicht --extensions=php --parallel=$(nproc)"
        ],
        "test": [
            "phpunit -c phpunit.xml.dist"
        ]
    }
}
